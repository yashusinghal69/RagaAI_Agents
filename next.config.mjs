/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  async headers() {
    return [
      {
        source: "/agents/:path*",
        headers: [
          {
            key: "Permissions-Policy",
            value:
              "microphone=*, camera=*, geolocation=*, gyroscope=*, magnetometer=*, accelerometer=*",
          },
          {
            key: "X-Frame-Options",
            value: "SAMEORIGIN",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
